<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profil Utilisateur</title>
<style>
  body {
    font-family: 'Arial', sans-serif;
background-image: url(../public/images/bgins.jpg);
background-size: cover;
    color: #333;
    margin: 0;
    padding: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
}

h1 {
    color:white;
    font-size: 32px;
    margin-bottom: 20px;
    text-align: center;
}

.table {
    width: 100%;
    max-width: 900px; /* زيادة الحجم الأقصى ليتناسب مع المحتوى المتزايد */
    background-color: #ffffff;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    overflow: hidden;
    margin-top: 20px;
}

table {
    width: 100%;
    border-collapse: collapse;
}

thead {
    background-color: #4674b6;
    color: #ffffff;
}

th,
td {
    padding: 15px;
    text-align: left;
}

th {
    text-transform: uppercase;
    font-weight: 600;
    letter-spacing: 0.05em;
}

tr:nth-child(even) {
    background-color: #f9f9f9; /* لون خلفية الصفوف الفردية */
}

tr:hover {
    background-color: #e0e7ff;
    transition: background-color 0.3s ease;
}

.titre {
    font-weight: bold;
    color: #4674b6;
}

td[colspan="2"] {
    text-align: center;
    color: #888;
    padding: 20px; 
}


th, td {
    border: 1px solid #4674b6;
}

thead th {
    background-color: #4674b6; 
    color: #ffffff;
}

</style>
</head>
<body>
    <h1>Événements auxquels vous avez participé</h1>
    <div class="table">
    <table>
        <thead>
            <tr>
                <th>Titre</th>
                <th>status</th>
                
            </tr>
        </thead>
        <tbody>
            <% if (evenements.length > 0) { %>
                <% evenements.forEach(function(evenement) { %>
                    <tr>
                        <td class="titre"><%= evenement.titre %></td>
                        <td><%= evenement.status %></td>
                        
                    </tr>
                <% }) %>
            <% } else { %>
                <tr>
                    <td colspan="3">Aucun événement trouvé.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</div>
</body>
</html>

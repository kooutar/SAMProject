<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier les Événements</title>
    <link rel="stylesheet" href="/css/styles.css"> <!-- Assurez-vous que le chemin est correct -->
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 20px;
        }
        h1 {
            color: #333;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
        }
        th {
            background-color: #4f6d7a;
            color: white;
        }
        button {
            background-color: #e38e10;
            color: white;
            border: none;
            padding: 8px 12px;
            cursor: pointer;
            border-radius: 4px;
        }
        button:hover {
            background-color: #d1902f;
        }
    </style>
</head>
<body>
    <h1>Modifier les Événements</h1>

    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Titre</th>
                <th>Description</th>
                <th>Date de Début</th>
                <th>Date de Fin</th>
                <th>Lieu</th>
                <th>Image</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% evenements.forEach(function(evenement) { %>
                <tr>
                    <td><%= evenement.id %></td>
                    <td><%= evenement.titre %></td>
                    <td><%= evenement.description %></td>
                    <td><%= evenement.date_debut %></td>
                    <td><%= evenement.date_fin %></td>
                    <td><%= evenement.lieu %></td>
                    <td><%= evenement.image %></td>
                    <td>
                        <form action="/delete-evenement/<%= evenement.id %>" method="POST" style="display:inline;">
                            <button type="submit">Supprimer</button>
                        </form>
                        <a href="/edit-evenement/<%= evenement.id %>">
                            <button>Modifier</button>
                        </a>
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>
</body>
</html>
